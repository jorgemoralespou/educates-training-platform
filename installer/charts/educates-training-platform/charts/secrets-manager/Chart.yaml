apiVersion: v2
name: secrets-manager
description: |
  Educates secrets-manager component. Reconciles SecretCopier, SecretExporter,
  SecretImporter, and SecretInjector resources to propagate secrets across
  namespaces.
type: application
version: 4.0.0-alpha.1
appVersion: "3.7.1"
annotations:
  educates.dev/image-registry-host: "ghcr.io"
  educates.dev/image-registry-namespace: "educates"
kubeVersion: ">=1.31.0-0"
