name: Build and Publish Images

on:
  workflow_dispatch:
    inputs:
      target_platforms:
        type: choice
        description: Platforms
        options:
        - linux/amd64
        - linux/arm64
        - linux/amd64,linux/arm64
  push:
    tags:
      - "[0-9]+.[0-9]+.[0-9]+"
      - "[0-9]+.[0-9]+.[0-9]+-alpha.[0-9]+"
      - "[0-9]+.[0-9]+.[0-9]+-beta.[0-9]+"
      - "[0-9]+.[0-9]+.[0-9]+-rc.[0-9]+"

jobs:
  publish-generic-images:
    name: Publish
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write
      id-token: write

    strategy:
      fail-fast: false
      matrix:
        include:
          - image: docker-registry
          - image: pause-container
          - image: session-manager
          - image: training-portal
          - image: secrets-manager
          - image: tunnel-manager
          - image: image-cache
          - image: assets-server
          - image: lookup-service
          - image: node-ca-injector
          # The v4 operator lives outside the <image-name>/ convention,
          # so it carries an explicit build context.
          - image: operator
            context: installer/operator

    steps:
      - name: Check out the repository
        uses: actions/checkout@v6

      - name: Calculate platforms
        shell: bash
        run: |
          TARGET_PLATFORMS="${{github.event.inputs.target_platforms}}"
          if [ -z "$TARGET_PLATFORMS" ]; then
            TARGET_PLATFORMS="${{secrets.TARGET_PLATFORMS}}"
          fi
          # Only build for amd64 if it's a fork for speed reasons
          if [ -z "$TARGET_PLATFORMS" ]; then
            IS_FORK=$(if [[ $GITHUB_EVENT_NAME == 'push' && $GITHUB_EVENT_PATH ]]; then jq -r .repository.fork <<< $(cat $GITHUB_EVENT_PATH); else echo false; fi)
            if [ $IS_FORK == "true" ]; then
              TARGET_PLATFORMS="linux/amd64"
            else
              TARGET_PLATFORMS="linux/amd64,linux/arm64"
            fi
          fi
          echo "TARGET_PLATFORMS=${TARGET_PLATFORMS}" >>${GITHUB_ENV}

      - name: Calculate variables
        shell: bash
        run: |
          REPOSITORY_OWNER=${{github.repository_owner}}
          echo "REPOSITORY_OWNER=${REPOSITORY_OWNER,,}" >>${GITHUB_ENV}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker buildx
        uses: docker/setup-buildx-action@v4

      - name: Free Disk Space
        if: ${{ (matrix.image == 'secrets-manager') || (matrix.image == 'session-manager') || (matrix.image == 'training-portal') || (matrix.image == 'tunnel-manager') || (matrix.image == 'lookup-service') }}
        uses: endersonmenezes/free-disk-space@v3
        with:
          remove_android: true
          remove_dotnet: true
          remove_haskell: true
          remove_tool_cache: true

      - name: Restore Docker cache
        if: ${{ (matrix.image == 'secrets-manager') || (matrix.image == 'session-manager') || (matrix.image == 'training-portal') || (matrix.image == 'tunnel-manager') || (matrix.image == 'lookup-service') }}
        uses: actions/cache/restore@v5
        with:
          path: /tmp/.buildx-cache-amd64-new
          key: ${{runner.os}}-buildx-cache-${{matrix.image}}-${{github.sha}}
          restore-keys: |
            ${{runner.os}}-buildx-cache-${{matrix.image}}-

      - name: Rename cache directories
        run: |
          test -d /tmp/.buildx-cache-new && mv /tmp/.buildx-cache-new /tmp/.buildx-cache-old || true
          du -ks /tmp/.buildx-cache-* || true

      - name: Generate container image metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: |
            ghcr.io/${{env.REPOSITORY_OWNER}}/educates-${{matrix.image}}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha

      - name: Login to GitHub container registry
        uses: docker/login-action@v4
        with:
          username: ${{github.actor}}
          password: ${{secrets.GITHUB_TOKEN}}
          registry: ghcr.io

      # On release tags the operator image must embed the runtime
      # subchart tarballs at the released version (versions are stamped
      # in CI, not committed — see decisions.md).
      - name: Stamp release version into operator chart embeds
        if: matrix.image == 'operator' && startsWith(github.ref, 'refs/tags/')
        shell: bash
        run: ./hack/stamp-release-version.sh "${GITHUB_REF##*/}" ghcr.io "${{env.REPOSITORY_OWNER}}"

      - name: Build and push ${{matrix.image}} image
        uses: docker/build-push-action@v7
        with:
          context: ${{matrix.context || matrix.image}}
          platforms: ${{env.TARGET_PLATFORMS}}
          tags: ${{steps.meta.outputs.tags}}
          cache-from: type=local,src=/tmp/.buildx-cache-old
          cache-to: type=local,mode=max,dest=/tmp/.buildx-cache-new
          push: true

      - name: Remove old cache directories
        run: |
          rm -rf /tmp/.buildx-cache-old || true

      - name: Dump cache directory sizes
        if: ${{ (matrix.image == 'secrets-manager') || (matrix.image == 'session-manager') || (matrix.image == 'training-portal') || (matrix.image == 'tunnel-manager') || (matrix.image == 'lookup-service') }}
        run: |
          du -ks /tmp/.buildx-cache-* || true

      - name: Save Docker cache
        if: ${{ (matrix.image == 'secrets-manager') || (matrix.image == 'session-manager') || (matrix.image == 'training-portal') || (matrix.image == 'tunnel-manager') || (matrix.image == 'lookup-service') }}
        uses: actions/cache/save@v5
        with:
          path: /tmp/.buildx-cache-new
          key: ${{runner.os}}-buildx-cache-${{matrix.image}}-${{github.sha}}

  publish-workshop-base-image:
    name: Publish (base-environment)
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
      - name: Free Disk Space
        uses: endersonmenezes/free-disk-space@v3
        with:
          remove_android: true
          remove_dotnet: true
          remove_haskell: true
          remove_tool_cache: true

      - name: Check out the repository
        uses: actions/checkout@v6

      - name: Calculate platforms
        shell: bash
        run: |
          TARGET_PLATFORMS="${{github.event.inputs.target_platforms}}"
          if [ -z "$TARGET_PLATFORMS" ]; then
            TARGET_PLATFORMS="${{secrets.TARGET_PLATFORMS}}"
          fi
          # Only build for amd64 if it's a fork for speed reasons
          if [ -z "$TARGET_PLATFORMS" ]; then
            IS_FORK=$(if [[ $GITHUB_EVENT_NAME == 'push' && $GITHUB_EVENT_PATH ]]; then jq -r .repository.fork <<< $(cat $GITHUB_EVENT_PATH); else echo false; fi)
            if [ $IS_FORK == "true" ]; then
              TARGET_PLATFORMS="linux/amd64"
            else
              TARGET_PLATFORMS="linux/amd64,linux/arm64"
            fi
          fi
          echo "TARGET_PLATFORMS=${TARGET_PLATFORMS}" >>${GITHUB_ENV}

      - name: Calculate variables
        shell: bash
        run: |
          REPOSITORY_OWNER=${{github.repository_owner}}
          echo "REPOSITORY_OWNER=${REPOSITORY_OWNER,,}" >>${GITHUB_ENV}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker buildx
        uses: docker/setup-buildx-action@v4

      - name: Verify docker daemon image contents
        run: |
          docker images

      - name: Restore Docker cache
        uses: actions/cache/restore@v5
        with:
          path: /tmp/.buildx-cache-new
          key: ${{runner.os}}-buildx-cache-base-environment-${{github.sha}}
          restore-keys: |
            ${{runner.os}}-buildx-cache-base-environment-


      - name: Rename cache directories
        run: |
          test -d /tmp/.buildx-cache-new && mv /tmp/.buildx-cache-new /tmp/.buildx-cache-old || true
          du -ks /tmp/.buildx-cache-* || true

      - name: Generate container image metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: |
            ghcr.io/${{env.REPOSITORY_OWNER}}/educates-base-environment
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha

      - name: Login to GitHub container registry
        uses: docker/login-action@v4
        with:
          username: ${{github.actor}}
          password: ${{secrets.GITHUB_TOKEN}}
          registry: ghcr.io

      - name: Build and push base-environment image
        uses: docker/build-push-action@v7
        with:
          context: workshop-images/base-environment
          platforms: ${{env.TARGET_PLATFORMS}}
          tags: ${{steps.meta.outputs.tags}}
          cache-from: type=local,src=/tmp/.buildx-cache-old
          cache-to: type=local,mode=max,dest=/tmp/.buildx-cache-new
          push: true

      - name: Remove old cache directories amd64
        run: |
          rm -rf /tmp/.buildx-cache-old || true

      - name: Dump cache directory sizes
        run: |
          du -ks /tmp/.buildx-cache-* || true

      - name: Save Docker cache
        uses: actions/cache/save@v5
        with:
          path: /tmp/.buildx-cache-new
          key: ${{runner.os}}-buildx-cache-base-environment-${{github.sha}}

  publish-workshop-images:
    name: Publish
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write
      id-token: write

    needs:
      - publish-workshop-base-image

    strategy:
      fail-fast: false
      matrix:
        include:
          - image: jdk8-environment
          - image: jdk11-environment
          - image: jdk17-environment
          - image: jdk21-environment
          - image: conda-environment

    steps:
      - name: Free Disk Space
        uses: endersonmenezes/free-disk-space@v3
        with:
          remove_android: true
          remove_dotnet: true
          remove_haskell: true
          remove_tool_cache: true

      - name: Check out the repository
        uses: actions/checkout@v6

      - name: Calculate platforms
        shell: bash
        run: |
          TARGET_PLATFORMS="${{github.event.inputs.target_platforms}}"
          if [ -z "$TARGET_PLATFORMS" ]; then
            TARGET_PLATFORMS="${{secrets.TARGET_PLATFORMS}}"
          fi
          # Only build for amd64 if it's a fork for speed reasons
          if [ -z "$TARGET_PLATFORMS" ]; then
            IS_FORK=$(if [[ $GITHUB_EVENT_NAME == 'push' && $GITHUB_EVENT_PATH ]]; then jq -r .repository.fork <<< $(cat $GITHUB_EVENT_PATH); else echo false; fi)
            if [ $IS_FORK == "true" ]; then
              TARGET_PLATFORMS="linux/amd64"
            else
              TARGET_PLATFORMS="linux/amd64,linux/arm64"
            fi
          fi
          echo "TARGET_PLATFORMS=${TARGET_PLATFORMS}" >>${GITHUB_ENV}

      - name: Calculate variables
        shell: bash
        run: |
          REPOSITORY_OWNER=${{github.repository_owner}}
          echo "REPOSITORY_OWNER=${REPOSITORY_OWNER,,}" >>${GITHUB_ENV}
          echo "REPOSITORY_SHA_TAG=sha-${GITHUB_SHA::7}" >>${GITHUB_ENV}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker buildx
        uses: docker/setup-buildx-action@v4

      - name: Verify docker daemon image contents
        run: |
          docker images

      - name: Restore Docker cache
        if: ${{ (matrix.image == 'conda-environment') }}
        uses: actions/cache/restore@v5
        with:
          path: /tmp/.buildx-cache-amd64-new
          key: ${{runner.os}}-buildx-cache-${{matrix.image}}-${{github.sha}}
          restore-keys: |
            ${{runner.os}}-buildx-cache-${{matrix.image}}-

      - name: Rename cache directories
        run: |
          test -d /tmp/.buildx-cache-new && mv /tmp/.buildx-cache-new /tmp/.buildx-cache-old || true
          du -ks /tmp/.buildx-cache-* || true

      - name: Generate container image metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: |
            ghcr.io/${{env.REPOSITORY_OWNER}}/educates-${{matrix.image}}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha

      - name: Login to GitHub container registry
        uses: docker/login-action@v4
        with:
          username: ${{github.actor}}
          password: ${{secrets.GITHUB_TOKEN}}
          registry: ghcr.io

      - name: Build and push ${{matrix.image}} image
        uses: docker/build-push-action@v7
        with:
          context: workshop-images/${{matrix.image}}
          platforms: ${{env.TARGET_PLATFORMS}}
          tags: ${{steps.meta.outputs.tags}}
          build-args: |
            IMAGE_REPOSITORY=ghcr.io/${{env.REPOSITORY_OWNER}}
            BASE_IMAGE_NAME=educates-base-environment
            PACKAGE_VERSION=${{env.REPOSITORY_SHA_TAG}}
          cache-from: type=local,src=/tmp/.buildx-cache-old
          cache-to: type=local,mode=max,dest=/tmp/.buildx-cache-new
          push: true

      - name: Remove old cache directories
        run: |
          rm -rf /tmp/.buildx-cache-old || true

      - name: Dump cache directory sizes
        run: |
          du -ks /tmp/.buildx-cache-* || true

      - name: Save Docker cache
        uses: actions/cache/save@v5
        with:
          path: /tmp/.buildx-cache-new
          key: ${{runner.os}}-buildx-cache-${{matrix.image}}-${{github.sha}}

  build-client-programs-linux-amd64:
    name: Build (clients) / amd64@linux
    runs-on: ubuntu-latest

    steps:
      - name: Check out the repository
        uses: actions/checkout@v6

      - name: Calculate variables
        shell: bash
        run: |
          REPOSITORY_OWNER=${{github.repository_owner}}
          echo "REPOSITORY_OWNER=${REPOSITORY_OWNER,,}" >>${GITHUB_ENV}
          echo "REPOSITORY_TAG=${GITHUB_REF##*/}" >>${GITHUB_ENV}

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.work
          cache-dependency-path: |
            client-programs/go.sum

      # On release tags the chart embedded in the CLI must carry the
      # released version and this fork's registry annotations.
      - name: Stamp release version into embedded chart
        if: startsWith(github.ref, 'refs/tags/')
        shell: bash
        run: ./hack/stamp-release-version.sh "${{env.REPOSITORY_TAG}}" ghcr.io "${{env.REPOSITORY_OWNER}}" --charts-only

      - name: Build educates client program
        shell: bash
        run: |
          rm -rf client-programs/pkg/renderer/files
          mkdir client-programs/pkg/renderer/files
          cp -rp workshop-images/base-environment/opt/eduk8s/etc/themes client-programs/pkg/renderer/files/
          cd client-programs
          REPOSITORY_TAG=${{env.REPOSITORY_TAG}}
          IMAGE_REPOSITORY=ghcr.io/${{env.REPOSITORY_OWNER}}
          GIT_COMMIT=${GITHUB_SHA::7}
          GIT_COMMIT_DATE=$(git show -s --format=%cd --date=short)
          go build -o educates-linux-amd64 -ldflags "-X 'main.projectVersion=$REPOSITORY_TAG' -X 'main.imageRepository=$IMAGE_REPOSITORY' -X 'main.gitCommit=$GIT_COMMIT' -X 'main.buildDate=$GIT_COMMIT_DATE'" cmd/educates/main.go

      - uses: actions/upload-artifact@v7
        with:
          name: educates-linux-amd64
          path: client-programs/educates-linux-amd64

  build-client-programs-linux-arm64:
    name: Build (clients) / arm64@linux
    runs-on: ubuntu-latest

    steps:
      - name: Check out the repository
        uses: actions/checkout@v6

      - name: Calculate variables
        shell: bash
        run: |
          REPOSITORY_OWNER=${{github.repository_owner}}
          echo "REPOSITORY_OWNER=${REPOSITORY_OWNER,,}" >>${GITHUB_ENV}
          echo "REPOSITORY_TAG=${GITHUB_REF##*/}" >>${GITHUB_ENV}

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.work
          cache-dependency-path: |
            client-programs/go.sum

      # On release tags the chart embedded in the CLI must carry the
      # released version and this fork's registry annotations.
      - name: Stamp release version into embedded chart
        if: startsWith(github.ref, 'refs/tags/')
        shell: bash
        run: ./hack/stamp-release-version.sh "${{env.REPOSITORY_TAG}}" ghcr.io "${{env.REPOSITORY_OWNER}}" --charts-only

      - name: Build educates client program
        shell: bash
        run: |
          rm -rf client-programs/pkg/renderer/files
          mkdir client-programs/pkg/renderer/files
          cp -rp workshop-images/base-environment/opt/eduk8s/etc/themes client-programs/pkg/renderer/files/
          cd client-programs
          REPOSITORY_TAG=${{env.REPOSITORY_TAG}}
          IMAGE_REPOSITORY=ghcr.io/${{env.REPOSITORY_OWNER}}
          GIT_COMMIT=${GITHUB_SHA::7}
          GIT_COMMIT_DATE=$(git show -s --format=%cd --date=short)
          GOOS=linux GOARCH=arm64 go build -o educates-linux-arm64 -ldflags "-X 'main.projectVersion=$REPOSITORY_TAG' -X 'main.imageRepository=$IMAGE_REPOSITORY' -X 'main.gitCommit=$GIT_COMMIT' -X 'main.buildDate=$GIT_COMMIT_DATE'" cmd/educates/main.go

      - uses: actions/upload-artifact@v7
        with:
          name: educates-linux-arm64
          path: client-programs/educates-linux-arm64

  build-client-programs-darwin-amd64:
    name: Build (clients) / amd64@darwin
    runs-on: macos-latest

    steps:
      - name: Check out the repository
        uses: actions/checkout@v6

      - name: Calculate variables
        shell: bash
        run: |
          REPOSITORY_OWNER=${{github.repository_owner}}
          echo "REPOSITORY_OWNER=$(echo "$REPOSITORY_OWNER" | tr '[:upper:]' '[:lower:]')" >>${GITHUB_ENV}
          echo "REPOSITORY_TAG=${GITHUB_REF##*/}" >>${GITHUB_ENV}

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.work
          cache-dependency-path: |
            client-programs/go.sum

      # On release tags the chart embedded in the CLI must carry the
      # released version and this fork's registry annotations.
      - name: Stamp release version into embedded chart
        if: startsWith(github.ref, 'refs/tags/')
        shell: bash
        run: ./hack/stamp-release-version.sh "${{env.REPOSITORY_TAG}}" ghcr.io "${{env.REPOSITORY_OWNER}}" --charts-only

      - name: Build educates client program
        shell: bash
        run: |
          rm -rf client-programs/pkg/renderer/files
          mkdir client-programs/pkg/renderer/files
          cp -rp workshop-images/base-environment/opt/eduk8s/etc/themes client-programs/pkg/renderer/files/
          cd client-programs
          REPOSITORY_TAG=${{env.REPOSITORY_TAG}}
          IMAGE_REPOSITORY=ghcr.io/${{env.REPOSITORY_OWNER}}
          GIT_COMMIT=${GITHUB_SHA::7}
          GIT_COMMIT_DATE=$(git show -s --format=%cd --date=short)
          # Need to build for amd64 on darwin on an arm64 runner as they are deprecating amd64 runner.
          GOOS=darwin GOARCH=amd64 go build -o educates-darwin-amd64 -ldflags "-X 'main.projectVersion=$REPOSITORY_TAG' -X 'main.imageRepository=$IMAGE_REPOSITORY' -X 'main.gitCommit=$GIT_COMMIT' -X 'main.buildDate=$GIT_COMMIT_DATE'" cmd/educates/main.go

      - uses: actions/upload-artifact@v7
        with:
          name: educates-darwin-amd64
          path: client-programs/educates-darwin-amd64

  build-client-programs-darwin-arm64:
    name: Build (clients) / arm64@darwin
    runs-on: macos-latest

    steps:
      - name: Check out the repository
        uses: actions/checkout@v6

      - name: Calculate variables
        shell: bash
        run: |
          REPOSITORY_OWNER=${{github.repository_owner}}
          echo "REPOSITORY_OWNER=$(echo "$REPOSITORY_OWNER" | tr '[:upper:]' '[:lower:]')" >>${GITHUB_ENV}
          echo "REPOSITORY_TAG=${GITHUB_REF##*/}" >>${GITHUB_ENV}

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.work
          cache-dependency-path: |
            client-programs/go.sum

      # On release tags the chart embedded in the CLI must carry the
      # released version and this fork's registry annotations.
      - name: Stamp release version into embedded chart
        if: startsWith(github.ref, 'refs/tags/')
        shell: bash
        run: ./hack/stamp-release-version.sh "${{env.REPOSITORY_TAG}}" ghcr.io "${{env.REPOSITORY_OWNER}}" --charts-only

      - name: Build educates client program
        shell: bash
        run: |
          rm -rf client-programs/pkg/renderer/files
          mkdir client-programs/pkg/renderer/files
          cp -rp workshop-images/base-environment/opt/eduk8s/etc/themes client-programs/pkg/renderer/files/
          cd client-programs
          REPOSITORY_TAG=${{env.REPOSITORY_TAG}}
          IMAGE_REPOSITORY=ghcr.io/${{env.REPOSITORY_OWNER}}
          GIT_COMMIT=${GITHUB_SHA::7}
          GIT_COMMIT_DATE=$(git show -s --format=%cd --date=short)
          go build -o educates-darwin-arm64 -ldflags "-X 'main.projectVersion=$REPOSITORY_TAG' -X 'main.imageRepository=$IMAGE_REPOSITORY' -X 'main.gitCommit=$GIT_COMMIT' -X 'main.buildDate=$GIT_COMMIT_DATE'" cmd/educates/main.go

      - uses: actions/upload-artifact@v7
        with:
          name: educates-darwin-arm64
          path: client-programs/educates-darwin-arm64

  publish-cli-image:
    name: Publish (cli) image
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write
      id-token: write

    needs:
      - publish-workshop-base-image

    steps:
      - name: Check out the repository
        uses: actions/checkout@v6

      - name: Calculate variables
        shell: bash
        run: |
          REPOSITORY_OWNER=${{github.repository_owner}}
          echo "REPOSITORY_OWNER=${REPOSITORY_OWNER,,}" >>${GITHUB_ENV}
          echo "REPOSITORY_SHA_TAG=sha-${GITHUB_SHA::7}" >>${GITHUB_ENV}
          echo "REPOSITORY_TAG=${GITHUB_REF##*/}" >>${GITHUB_ENV}
          echo "GIT_COMMIT=${GITHUB_SHA::7}" >>${GITHUB_ENV}
          echo "GIT_COMMIT_DATE=$(git show -s --format=%cd --date=short)" >>${GITHUB_ENV}

      # On release tags the chart embedded in the CLI must carry the
      # released version and this fork's registry annotations.
      - name: Stamp release version into embedded chart
        if: startsWith(github.ref, 'refs/tags/')
        shell: bash
        run: ./hack/stamp-release-version.sh "${{env.REPOSITORY_TAG}}" ghcr.io "${{env.REPOSITORY_OWNER}}" --charts-only

      - name: Calculate platforms
        shell: bash
        run: |
          TARGET_PLATFORMS="${{github.event.inputs.target_platforms}}"
          if [ -z "$TARGET_PLATFORMS" ]; then
            TARGET_PLATFORMS="${{secrets.TARGET_PLATFORMS}}"
          fi
          # Only build for amd64 if it's a fork for speed reasons
          if [ -z "$TARGET_PLATFORMS" ]; then
            IS_FORK=$(if [[ $GITHUB_EVENT_NAME == 'push' && $GITHUB_EVENT_PATH ]]; then jq -r .repository.fork <<< $(cat $GITHUB_EVENT_PATH); else echo false; fi)
            if [ $IS_FORK == "true" ]; then
              TARGET_PLATFORMS="linux/amd64"
            else
              TARGET_PLATFORMS="linux/amd64,linux/arm64"
            fi
          fi
          echo "TARGET_PLATFORMS=${TARGET_PLATFORMS}" >>${GITHUB_ENV}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker buildx
        uses: docker/setup-buildx-action@v3

      - name: Generate container image metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: |
            ghcr.io/${{env.REPOSITORY_OWNER}}/educates-cli
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha

      - name: Login to GitHub container registry
        uses: docker/login-action@v4
        with:
          username: ${{github.actor}}
          password: ${{secrets.GITHUB_TOKEN}}
          registry: ghcr.io

      - name: Build and push docker-cli image
        uses: docker/build-push-action@v7
        with:
          context: client-programs
          platforms: ${{env.TARGET_PLATFORMS}}
          build-args: |
            REPOSITORY=ghcr.io/${{env.REPOSITORY_OWNER}}
            TAG=${{env.REPOSITORY_SHA_TAG}}
            PROJECT_VERSION=${{env.REPOSITORY_TAG}}
            IMAGE_REPOSITORY=ghcr.io/${{env.REPOSITORY_OWNER}}
            GIT_COMMIT=${{env.GIT_COMMIT}}
            GIT_COMMIT_DATE=${{env.GIT_COMMIT_DATE}}
          tags: ${{steps.meta.outputs.tags}}
          push: true

  publish-docker-extension:
    name: Extension
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write
      id-token: write

    needs:
      - publish-cli-image

    steps:
      - name: Check out the repository
        uses: actions/checkout@v6

      - name: Calculate variables
        shell: bash
        run: |
          REPOSITORY_OWNER=${{github.repository_owner}}
          echo "REPOSITORY_OWNER=${REPOSITORY_OWNER,,}" >>${GITHUB_ENV}
          echo "REPOSITORY_SHA_TAG=sha-${GITHUB_SHA::7}" >>${GITHUB_ENV}

      - name: Calculate platforms
        shell: bash
        run: |
          TARGET_PLATFORMS="${{github.event.inputs.target_platforms}}"
          if [ -z "$TARGET_PLATFORMS" ]; then
            TARGET_PLATFORMS="${{secrets.TARGET_PLATFORMS}}"
          fi
          # Only build for amd64 if it's a fork for speed reasons
          if [ -z "$TARGET_PLATFORMS" ]; then
            IS_FORK=$(if [[ $GITHUB_EVENT_NAME == 'push' && $GITHUB_EVENT_PATH ]]; then jq -r .repository.fork <<< $(cat $GITHUB_EVENT_PATH); else echo false; fi)
            if [ $IS_FORK == "true" ]; then
              TARGET_PLATFORMS="linux/amd64"
            else
              TARGET_PLATFORMS="linux/amd64,linux/arm64"
            fi
          fi
          echo "TARGET_PLATFORMS=${TARGET_PLATFORMS}" >>${GITHUB_ENV}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker buildx
        uses: docker/setup-buildx-action@v4

      - name: Generate container image metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: |
            ghcr.io/${{env.REPOSITORY_OWNER}}/educates-docker-extension
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha

      - name: Login to GitHub container registry
        uses: docker/login-action@v4
        with:
          username: ${{github.actor}}
          password: ${{secrets.GITHUB_TOKEN}}
          registry: ghcr.io

      - name: Build and push docker-extension image
        uses: docker/build-push-action@v7
        with:
          context: docker-extension
          platforms: ${{env.TARGET_PLATFORMS}}
          build-args: |
            REPOSITORY=ghcr.io/${{env.REPOSITORY_OWNER}}
            TAG=${{env.REPOSITORY_SHA_TAG}}
          tags: ${{steps.meta.outputs.tags}}
          push: true

  publish-charts:
    name: Publish (helm charts)
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')

    permissions:
      contents: read
      packages: write

    # Charts reference the images this run publishes; don't push charts
    # for a release whose images failed.
    needs:
      - publish-generic-images

    steps:
      - name: Check out the repository
        uses: actions/checkout@v6

      - name: Calculate variables
        shell: bash
        run: |
          REPOSITORY_OWNER=${{github.repository_owner}}
          echo "REPOSITORY_OWNER=${REPOSITORY_OWNER,,}" >>${GITHUB_ENV}
          echo "REPOSITORY_TAG=${GITHUB_REF##*/}" >>${GITHUB_ENV}

      - name: Stamp release version
        shell: bash
        run: ./hack/stamp-release-version.sh "${{env.REPOSITORY_TAG}}" ghcr.io "${{env.REPOSITORY_OWNER}}"

      - name: Package charts
        shell: bash
        run: |
          mkdir -p dist
          helm package installer/charts/educates-installer --destination dist
          helm package installer/charts/educates-training-platform --destination dist

      - name: Login to GitHub container registry
        shell: bash
        run: |
          echo "${{secrets.GITHUB_TOKEN}}" | helm registry login ghcr.io \
            --username ${{github.actor}} --password-stdin

      - name: Push charts to OCI registry
        shell: bash
        run: |
          helm push dist/educates-installer-${{env.REPOSITORY_TAG}}.tgz \
            oci://ghcr.io/${{env.REPOSITORY_OWNER}}/charts
          helm push dist/educates-training-platform-${{env.REPOSITORY_TAG}}.tgz \
            oci://ghcr.io/${{env.REPOSITORY_OWNER}}/charts

      - uses: actions/upload-artifact@v7
        with:
          name: helm-charts
          path: dist/*.tgz

  publish-schemas:
    name: Publish (json schemas)
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    # Deploys to this repository's GitHub Pages site (upstream maps it
    # to schemas.educates.dev — see developer-docs/json-schemas.md).
    # Forks without Pages enabled fail the deploy step; that must not
    # gate the release.
    continue-on-error: true

    permissions:
      contents: read
      pages: write
      id-token: write

    environment:
      name: github-pages
      url: ${{steps.deployment.outputs.page_url}}

    steps:
      - name: Check out the repository
        uses: actions/checkout@v6

      # Published filenames drop the .schema infix to match the $id
      # baked into each schema (…/cli/v1alpha1/<Kind>.json).
      - name: Stage schemas
        shell: bash
        run: |
          mkdir -p site/cli/v1alpha1
          for f in client-programs/pkg/config/v1alpha1/schemas/*.schema.json; do
            name=$(basename "$f" .schema.json)
            cp "$f" "site/cli/v1alpha1/$name.json"
          done
          {
            echo '<!DOCTYPE html><html><head><title>Educates CLI JSON schemas</title></head><body>'
            echo '<h1>Educates CLI JSON schemas (cli.educates.dev/v1alpha1)</h1><ul>'
            for f in site/cli/v1alpha1/*.json; do
              echo "<li><a href=\"cli/v1alpha1/$(basename "$f")\">$(basename "$f")</a></li>"
            done
            echo '</ul></body></html>'
          } > site/index.html

      - name: Setup Pages
        uses: actions/configure-pages@v6

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5

  publish-image-list:
    name: Publish (image list)
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')

    permissions:
      contents: read
      packages: read

    # Digest resolution needs every listed image already pushed.
    needs:
      - publish-generic-images
      - publish-workshop-base-image

    steps:
      - name: Check out the repository
        uses: actions/checkout@v6

      - name: Calculate variables
        shell: bash
        run: |
          REPOSITORY_OWNER=${{github.repository_owner}}
          echo "REPOSITORY_OWNER=${REPOSITORY_OWNER,,}" >>${GITHUB_ENV}
          echo "REPOSITORY_TAG=${GITHUB_REF##*/}" >>${GITHUB_ENV}

      # Digest-pinned list of every image an air-gapped install needs
      # (platform images + base-environment + upstream cluster-service
      # images from the vendored charts). Attached to the GitHub
      # release for name-preserving mirroring with skopeo/crane.
      - name: Generate image list
        shell: bash
        run: |
          skopeo login ghcr.io --username ${{github.actor}} --password ${{secrets.GITHUB_TOKEN}}
          ./hack/generate-image-list.sh "${{env.REPOSITORY_TAG}}" ghcr.io "${{env.REPOSITORY_OWNER}}" \
            > educates-images-${{env.REPOSITORY_TAG}}.txt
          cat educates-images-${{env.REPOSITORY_TAG}}.txt

      - uses: actions/upload-artifact@v7
        with:
          name: image-list
          path: educates-images-*.txt

  release-artifacts:
    name: Release
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    needs:
      - build-client-programs-linux-amd64
      - build-client-programs-linux-arm64
      - build-client-programs-darwin-amd64
      - build-client-programs-darwin-arm64
      - publish-cli-image
      - publish-docker-extension
      - publish-charts
      - publish-image-list

    steps:
      - name: Calculate variables
        shell: bash
        run: |
          REPOSITORY_TAG="${GITHUB_REF##*/}"
          if [[ "$REPOSITORY_TAG" == *-* ]]; then
            PRERELEASE=true
          else
            PRERELEASE=false
          fi
          echo "REPOSITORY_TAG=${REPOSITORY_TAG}" >>${GITHUB_ENV}
          echo "PRERELEASE=${PRERELEASE}" >>${GITHUB_ENV}

      - name: Restore educates-linux-amd64
        uses: actions/download-artifact@v8
        with:
          name: educates-linux-amd64

      - name: Restore educates-linux-arm64
        uses: actions/download-artifact@v8
        with:
          name: educates-linux-arm64

      - name: Restore educates-darwin-amd64
        uses: actions/download-artifact@v8
        with:
          name: educates-darwin-amd64

      - name: Restore educates-darwin-arm64
        uses: actions/download-artifact@v8
        with:
          name: educates-darwin-arm64

      - name: Restore helm charts
        uses: actions/download-artifact@v8
        with:
          name: helm-charts

      - name: Restore image list
        uses: actions/download-artifact@v8
        with:
          name: image-list

      - name: Generate file checksums for CLI binaries
        shell: bash
        run: |
          sha256sum educates-darwin-amd64 >> checksums.txt
          sha256sum educates-darwin-arm64 >> checksums.txt
          sha256sum educates-linux-amd64 >> checksums.txt
          sha256sum educates-linux-arm64 >> checksums.txt
          sha256sum educates-installer-${{env.REPOSITORY_TAG}}.tgz >> checksums.txt
          sha256sum educates-training-platform-${{env.REPOSITORY_TAG}}.tgz >> checksums.txt
          sha256sum educates-images-${{env.REPOSITORY_TAG}}.txt >> checksums.txt
          echo 'File Checksums' >> release-notes.md
          echo '--------------' >> release-notes.md
          echo '```' >> release-notes.md
          cat checksums.txt >> release-notes.md
          echo '```' >> release-notes.md

      - uses: actions/upload-artifact@v7
        with:
          name: checksums.txt
          path: checksums.txt

      - uses: actions/upload-artifact@v7
        with:
          name: release-notes.md
          path: release-notes.md

      - name: Create release
        id: create_release
        uses: softprops/action-gh-release@v3
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
        with:
          tag_name: ${{env.REPOSITORY_TAG}}
          name: "educates:${{env.REPOSITORY_TAG}}"
          draft: false
          prerelease: ${{env.PRERELEASE}}
          body_path: release-notes.md
          files: |
            checksums.txt
            educates-linux-amd64
            educates-linux-arm64
            educates-darwin-amd64
            educates-darwin-arm64
            educates-installer-*.tgz
            educates-training-platform-*.tgz
            educates-images-*.txt
