# {{ or .BundleName "Workshop Bundle" }}

Multi-workshop repository generated by Educates CLI.

Use `educates bundle publish` to publish workshop content and `educates bundle export` to export all resource definitions.
